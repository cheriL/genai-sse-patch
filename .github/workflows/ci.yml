name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]
        google-genai-version: ["1.0.x", "2.x"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install "google-genai==${{ matrix.google-genai-version }}" pytest pytest-asyncio httpx aiohttp
      - run: pip install -e .
      - run: pytest -v
